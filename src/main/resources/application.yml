server:
  port: 8080
spring:
  application:
    name: url-shortener
  datasource:
    url: jdbc:postgresql://localhost:5432/shortener
    username: postgres
    password: postgres
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
